namespace Baetoti.Shared.Enum
{
    public enum AppRole
    {
        Admin = 1,
        User = 2
    }

    public enum EmployementStatus
    {
        Active = 1,
        Inactive = 2
    }

    public enum DBSchema
    {
        baetoti,
        audit
    }

}
